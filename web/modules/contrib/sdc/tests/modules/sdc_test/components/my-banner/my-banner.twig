{# Prepare presentational attributes #}
{% if image is not empty %}
  {% set sdcAttributes = sdcAttributes.setAttribute('style', 'background-image: linear-gradient(to right, black, black, rgba(0, 0, 0, 70%), transparent), url("' ~ image ~ '");') %}
{% endif %}

{# Markup for the component #}
<div {{ sdcAttributes }}>
  <div class="sdc--my-banner--header">
    <h3>{{ heading }}</h3>
    {% include 'sdc_test:my-cta' with { text: ctaText, href: ctaHref, target: ctaTarget } only %}
  </div>
  <div class="sdc--my-banner--body">
    {% block banner_body %}
    {% endblock %}
  </div>
</div>
